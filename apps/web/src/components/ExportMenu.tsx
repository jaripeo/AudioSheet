/**
 * MusicXML, compressed .mxl, MIDI, practice MIDI and browser print-to-PDF. Serialisation happens in the export worker.
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function ExportMenu(): never {
  throw new Error("unimplemented: ExportMenu lands in Phase 2");
}
