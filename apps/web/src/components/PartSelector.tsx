/**
 * Part and stem selection, with solo and mute per stem. Stems gated out as silent are shown but disabled, with their W_STEM_QUIET reason.
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function PartSelector(): never {
  throw new Error("unimplemented: PartSelector lands in Phase 2");
}
