/**
 * WaveSurfer waveform with the beat grid and per-note transcription confidence overlaid, so the user can see where the pipeline was unsure.
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function WaveformStrip(): never {
  throw new Error("unimplemented: WaveformStrip lands in Phase 2");
}
