/**
 * Standard notation via the VexFlow adapter. Systems are virtualised to +/- 2 viewport heights (Section 4.3); ghost notes are drawn at 25% opacity when show_ghost_notes is on.
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function ScoreView(): never {
  throw new Error("unimplemented: ScoreView lands in Phase 2");
}
