/**
 * Every E_* and W_* the run produced, plus the document stats and the raw piano roll debug view used from Phase 5 onward.
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function DiagnosticsDrawer(): never {
  throw new Error("unimplemented: DiagnosticsDrawer lands in Phase 2");
}
