/**
 * Click a note to see its origin_ids, raw onset in milliseconds, confidence and applied quantisation shift. This makes the pipeline auditable by the user and is the fastest debugging tool the team has (Section 4.4).
 *
 * Phase 2 implements this (ARCHITECTURE.md Section 5.3). The file exists now so
 * the Section 5.1 tree is complete.
 */

/**
 * Render the component.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function NoteInspector(): never {
  throw new Error("unimplemented: NoteInspector lands in Phase 2");
}
