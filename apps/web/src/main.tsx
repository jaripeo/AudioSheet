/**
 * React entry point — ARCHITECTURE.md Section 4.1, Phase 2.
 *
 * Phase 2 installs React and Vite and implements this. The file exists now so the
 * Section 5.1 tree is complete.
 */

/**
 * Mount the application into `#root`.
 *
 * @throws Always in Phase 0; lands in Phase 2.
 */
export function mount(): void {
  throw new Error("unimplemented: the React entry point lands in Phase 2");
}
