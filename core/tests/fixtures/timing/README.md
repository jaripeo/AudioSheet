# `timing/` fixtures

Placeholder. See `docs/evaluation.md` for what this family contains and which gate
it serves. The audio is generated or acquired by the phase that needs it and is not
committed (see `.gitignore`).
